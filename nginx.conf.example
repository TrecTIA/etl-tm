# ──────────────────────────────────────────────────────────────
# Nginx config untuk ETL HCMS v2 API
# Simpan di VPS: /etc/nginx/sites-available/etl-hcms
# Lalu: sudo ln -s /etc/nginx/sites-available/etl-hcms /etc/nginx/sites-enabled/
# ──────────────────────────────────────────────────────────────

server {
    listen 80;
    server_name etl.yourdomain.com;    # ← ganti dengan domain/subdomain VPS Anda

    # Redirect HTTP → HTTPS (aktifkan setelah SSL terpasang)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://127.0.0.1:8010;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        # Timeout besar untuk upload file Excel yang besar
        proxy_read_timeout    300s;
        proxy_connect_timeout 60s;
        proxy_send_timeout    300s;

        # Batas ukuran file upload (sesuaikan dengan kebutuhan)
        client_max_body_size  50M;
    }
}

# ── HTTPS (setelah certbot dijalankan, blok ini akan diisi otomatis) ──
# server {
#     listen 443 ssl;
#     server_name etl.yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/etl.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/etl.yourdomain.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8010;
#         # ... (opsi yang sama dengan blok HTTP di atas)
#     }
# }
